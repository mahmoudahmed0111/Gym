{{-- <a href="{{ route('users.create') }}" style="width: fit-content"> --}}
    <button type="{{$type??"button"}}" class="btn btn-primary d-flex align-items-center gap-2"> <i class="fa-solid fa-plus"></i>
        {{ __("home.$name") }}
    </button>
{{-- </a> --}}
